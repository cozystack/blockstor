---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: resourcegroups.blockstor.io.blockstor.io
spec:
  group: blockstor.io.blockstor.io
  names:
    kind: ResourceGroup
    listKind: ResourceGroupList
    plural: resourcegroups
    singular: resourcegroup
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceGroup is the Schema for the resourcegroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ResourceGroup
            properties:
              description:
                description: description is human-readable.
                type: string
              peerSlots:
                description: peerSlots is the DRBD peer-slot count baked into spawned
                  resources.
                format: int32
                type: integer
              props:
                additionalProperties:
                  type: string
                description: props is the LINSTOR property map for this resource group.
                type: object
              selectFilter:
                description: |-
                  selectFilter constrains autoplacer when spawning resources from this
                  group.
                properties:
                  disklessOnRemaining:
                    type: boolean
                  layerStack:
                    items:
                      type: string
                    type: array
                  nodeNameList:
                    items:
                      type: string
                    type: array
                  notPlaceWithRsc:
                    items:
                      type: string
                    type: array
                  notPlaceWithRscRegex:
                    type: string
                  placeCount:
                    format: int32
                    type: integer
                  providerList:
                    items:
                      type: string
                    type: array
                  replicasOnDifferent:
                    items:
                      type: string
                    type: array
                  replicasOnSame:
                    items:
                      type: string
                    type: array
                  storagePool:
                    type: string
                  storagePoolDisklessList:
                    items:
                      type: string
                    type: array
                  storagePoolList:
                    items:
                      type: string
                    type: array
                type: object
              volumeGroups:
                description: volumeGroups templates the per-volume props/flags for
                  spawned resources.
                items:
                  description: ResourceGroupVolumeGroup is one volume's template inside
                    a ResourceGroup.
                  properties:
                    flags:
                      items:
                        type: string
                      type: array
                    props:
                      additionalProperties:
                        type: string
                      type: object
                    volumeNumber:
                      format: int32
                      type: integer
                  required:
                  - volumeNumber
                  type: object
                type: array
            type: object
          status:
            description: status defines the observed state of ResourceGroup
            properties:
              conditions:
                description: conditions represent the current state of the ResourceGroup.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
